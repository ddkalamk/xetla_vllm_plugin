diff --git a/examples/offline_inference/run_one_batch.py b/examples/offline_inference/run_one_batch.py
index d7692c563..26e5c5ec9 100644
--- a/examples/offline_inference/run_one_batch.py
+++ b/examples/offline_inference/run_one_batch.py
@@ -7,7 +7,7 @@ from vllm import LLM, EngineArgs
 from vllm.config import ProfilerConfig
 from vllm.utils.argparse_utils import FlexibleArgumentParser
 
-DEFAULT_MAX_TOKENS = 16
+DEFAULT_MAX_TOKENS = 128
 
 
 def create_parser() -> FlexibleArgumentParser:
@@ -17,8 +17,8 @@ def create_parser() -> FlexibleArgumentParser:
 
     batch_group = parser.add_argument_group("Batch parameters")
     batch_group.add_argument("--batch-size", type=int, default=1)
-    batch_group.add_argument("--prompt-size", type=int, default=128)
-    batch_group.add_argument("--prompt-prefix", type=str, default="Hello, my name is")
+    batch_group.add_argument("--prompt-size", type=int, default=32)
+    batch_group.add_argument("--prompt-prefix", type=str, default="Give me Bubble sort code in Python code with some test cases.")
 
     profile_group = parser.add_argument_group("Profiling parameters")
     profile_group.add_argument(
diff --git a/vllm/engine/arg_utils.py b/vllm/engine/arg_utils.py
index e1772ab1d..c933f97e5 100644
--- a/vllm/engine/arg_utils.py
+++ b/vllm/engine/arg_utils.py
@@ -1384,7 +1384,14 @@ class EngineArgs:
     def create_model_config(self) -> ModelConfig:
         # gguf file needs a specific model loader
         if is_gguf(self.model):
-            self.quantization = self.load_format = "gguf"
+            # Always force the GGUF model loader so weights come from the
+            # .gguf file. But only force quantization=gguf if the user did
+            # not explicitly request a different quantization scheme. This
+            # lets out-of-tree quantization plugins (e.g. xetla) re-quantize
+            # GGUF weights at load time.
+            self.load_format = "gguf"
+            if self.quantization is None:
+                self.quantization = "gguf"
 
         if not envs.VLLM_ENABLE_V1_MULTIPROCESSING:
             logger.warning(
diff --git a/vllm/model_executor/model_loader/weight_utils.py b/vllm/model_executor/model_loader/weight_utils.py
index 37023d3f1..9ab102d5c 100644
--- a/vllm/model_executor/model_loader/weight_utils.py
+++ b/vllm/model_executor/model_loader/weight_utils.py
@@ -265,6 +265,13 @@ def get_quant_config(
     if model_config.quantization == "gguf":
         return quant_cls()
 
+    # Out-of-tree xetla plugin: instantiated from environment variables, not
+    # from the HF model config. Skip the snapshot_download/JSON config lookup
+    # so we can re-quantize GGUF (and other) checkpoints with `--quantization
+    # xetla`.
+    if model_config.quantization == "xetla":
+        return quant_cls()
+
     # Read the quantization config from the HF model config, if available.
     hf_quant_config = getattr(model_config.hf_config, "quantization_config", None)
     # some vision model may keep quantization_config in their text_config
